// Main component
export { default as BottomNavigation } from '../BottomNavigation';

// Hooks
export { useBottomNavigation } from './useBottomNavigation';
export { useBottomNavVisibility } from './useBottomNavVisibility';

// Components
export { NavItem } from './NavItem';

// Types
export type { NavItem as NavItemType, BottomNavigationProps } from './types';

// Constants & Icons
export { NAVIGATION_ITEMS } from './constants';
export { HomeIcon, CartIcon, OrdersIcon, ProfileIcon } from './icons';
